import { Container, Typography, Box } from "@mui/material";
import React from "react";

const CreateTokenPage = () => {
    return (
        <Container sx={{
            flexDirection: "row-reverse",
            justifyContent: "flex-end",
            alignItems: "flex-end",
        }}>
            <h1>CreateToken page</h1>
        </Container>
    )
};

export default CreateTokenPage;